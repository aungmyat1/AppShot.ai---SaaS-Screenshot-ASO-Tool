# ========================================
# AppShot.ai - Staging defaults (optional)
# ========================================
# Safe defaults for staging/preview. Set secrets in Vercel or Doppler.
# Used when NODE_ENV=staging or for preview deployments.

NODE_ENV=production
APP_URL=https://your-preview-url.vercel.app
PORT=3000

# Queue: sync for Vercel preview
SCRAPE_QUEUE_MODE=sync
PLAY_SCRAPE_MODE=html
PLAY_SCRAPE_FALLBACK_PLAYWRIGHT=false
