# ========================================
# AppShot.ai - Production defaults (optional)
# ========================================
# Safe defaults only. All secrets must be set in Vercel or Doppler.
# Used for production builds (NODE_ENV=production).

NODE_ENV=production
PORT=3000

# Queue: sync for Vercel serverless (use "queue" only with dedicated worker)
SCRAPE_QUEUE_MODE=sync
PLAY_SCRAPE_MODE=html
PLAY_SCRAPE_FALLBACK_PLAYWRIGHT=false
