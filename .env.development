# ========================================
# AppShot.ai - Development defaults (optional)
# ========================================
# Safe defaults only. Override secrets in .env.local or use Doppler.
# Next.js loads: .env, .env.local, .env.development, .env.development.local

NODE_ENV=development
APP_URL=http://localhost:3000
PORT=3000

# Queue: sync recommended for local/Vercel serverless
SCRAPE_QUEUE_MODE=sync
PLAY_SCRAPE_MODE=html
PLAY_SCRAPE_FALLBACK_PLAYWRIGHT=false
